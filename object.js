/**
 * Membuat variable user menggunakan const
 * Membuat object menggunakan {}
 */

const user = {
    name: "Mark Lee",
    address: "Canada",
    age: 25,
    isMarried: true,
};

console.log(user.name);